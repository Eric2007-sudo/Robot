# 笔试题：相机标定与位姿解算

> 建议用时 2~3 小时。允许查阅资料，允许参考开源实现，也鼓励用 AI 辅助理解代码和排查报错。用 AI 辅助的部分标注一下即可。

> **这题有多大**
>
> - **要你写的**：`calibrate.py` 里 7 个 TODO（第 70、101、119、155、179、209、238 行），约 60 行代码
> - **已给你的**：参数解析、主流程、终端输出，约 220 行，不用改
> - **目录里 40 多个文件**：其中 41 张是标定图片，代码只有 `calibrate.py` 一个
> - **第一步**：直接 `python3 calibrate.py`，它会报"0 张图检测成功"，这是预期的，从这里开始填
> - **卡住了**：每个 TODO 上面的 docstring 写了做什么、用哪个函数，把它和报错一起提供给 AI 提问

## 一、背景

自瞄的完整链路是：相机拍到画面 → 检测器给出装甲板的像素坐标 → PnP 解出装甲板在相机坐标系下的位姿 → 换算成云台需要的 Yaw/Pitch → 开火。

这条链路从第三步开始依赖一组参数：**相机内参和畸变系数**。`solvePnP` 必须用到它们，缺了就无法解算。这组参数只能通过标定得到，它由镜头焦距、传感器像素尺寸和装配误差决定，每台相机各不相同，换镜头后需要重新标定。

内参不准不会让程序报错，而是让位姿解算结果整体偏移。近距离时偏差不明显，距离拉远后误差会放大。标定处在整条链路的最前端，出问题时也最难反查，因此值得先做扎实。

本题要求你写一份标定程序，跑通给定的图片，并判断结果是否正确。

## 二、需要先弄懂的内容

本题的知识点在入组培训文档里都有，动手前先通读：

> **[12-相机标定与位姿解算](https://hello-world-vision.github.io/Vision_Website/induction-training/12-相机标定与位姿解算/)**
>
> - **模块一** 小孔成像、内参外参、径向/切向畸变、张正友标定原理、重投影误差的量级参考 —— 对应任务 1~4
> - **模块三** PnP 位姿解算 —— 对应任务 5

看完后你应当能回答以下问题，做题时会用到：

- 内参矩阵里 `fx, fy, cx, cy` 各代表什么，为什么内参只与相机本身有关
- OpenCV 畸变系数 `[k1, k2, p1, p2, k3]` 的排列顺序，哪些描述径向畸变，哪些描述切向畸变
- 为什么标定需要多张不同姿态的图片，一张为什么不够
- 重投影误差多大算合格

### 标定板

本题用**棋盘格**，图片已随题下发。选标定板时有几点容易出错：

- **内部角点数指黑白交界点，不是方格数。** 一块 10x7 方格的板，内部角点是 9x6。这个参数最容易写错，写错后一张图也检测不出来。
- **长宽不要相等。** 9x6 可用，8x8 不可用：正方形板存在 90 度旋转歧义，同一块板在不同图片里的角点编号可能对不上。
- **方格边长要量准。** 它不影响内参和畸变系数，只决定外参的尺度，也就是任务 5 里 `tvec` 的单位。

圆点阵、ChArUco、AprilGrid 等其他标定板的适用场景，文档模块一里有对比表。

## 三、已提供的内容

```
Camera_Calibration/
├── QUESTION.md          # 本文件
├── calibrate.py         # [★ 主要工作] 六个 TODO（标定五个 + PnP 一个）
├── image/               # 41 张标定图片
└── result/              # [★ 你的产出] 标定结果写到这里
```

`calibrate.py` 是骨架代码，`parse_args()` 和 `main()` 已经写好，主流程的调用顺序可以照着看。你要填的是六个函数：

| 函数 | 要做的事 |
|------|---------|
| `build_object_points()` | 生成标定板坐标系下的理论角点坐标 |
| `find_corners()` | 角点检测 + 亚像素精确化 |
| `collect()` | 遍历图片，收集 3D-2D 对应 |
| `per_view_errors()` | 逐张重投影误差 |
| `write_result()` | 结果写入 YAML |
| `solve_pnp_all()` | PnP 位姿解算（任务 5） |

骨架可以直接运行，但函数还是空的，会在 `collect()` 里报"0 张图检测成功"并退出。填完才有结果。参数默认值里的 `--board` 是占位值，需要改成正确的规格。

`image/` 下是 41 张 `1280x720` 的 JPG，同一台相机、同一组镜头参数拍摄，文件名为 `0.jpg` ~ `40.jpg`。

标定板是方格边长 17 mm 的棋盘格，但**内部角点数需要你自己确定**。这不是刁难。实际接手别人拍摄的标定图时，规格往往没有记录，你需要有办法自己判断。提示：角点数写错时 `findChessboardCorners` 会全部检测失败，可以利用这一点。

## 四、任务

### 任务 1：实现标定流程（40 分）

填写 `calibrate.py` 里的 TODO 1~5，TODO 6 属于任务 5。完整流程是：

1. 遍历 `image/` 下的图片，转灰度
2. `findChessboardCorners` 检测角点
3. `cornerSubPix` 做亚像素精确化
4. 按标定板规格生成理论角点坐标（`Z=0` 平面）
5. `calibrateCamera` 求解内参、畸变系数、每张图的外参
6. `projectPoints` 逐张计算重投影误差
7. 结果写入 `result/calibration_result.yaml`

几点要求：

- **第 3 步不能省。** `findChessboardCorners` 只有整像素精度，省掉亚像素精化会让误差变大数倍。建议自己注释掉对比一次。
- **逐张误差要单独计算，不能只报 `calibrateCamera` 的返回值。** 那个返回值是所有角点合并后的总体 RMS，看不出是哪张图片拉低了精度。
- 检测失败的图片跳过并记录下来，不要让程序中断。
- 图片尺寸不一致时应当拒绝标定并说明原因。先想清楚混用不同分辨率标出的内参代表什么。

**评分关注**：流程是否完整正确、亚像素精化有没有做、边界情况是否处理、代码是否可读。

### 任务 2：参数外置（10 分）

`parse_args()` 已经给了一个够用的起点：图片目录、匹配模式、标定板规格、方格边长、输出路径都在里面。你至少要把 `--board` 的默认值改成正确的规格，使得直接跑 `python3 calibrate.py` 就能标定本题数据。

在此基础上按自己的实现增删参数。任务 4 如果做了验证图输出，对应的开关也应当外置。

要求是**任何标定相关的量都不能硬编码在函数体内**。判断标准是：换一批数据时这个值会不会变。也想一下哪些不必外置，比如 `SUBPIX_CRITERIA` 为什么可以固定写在模块级。

### 任务 3：结果输出（15 分）

结果写到 `result/calibration_result.yaml`，用 OpenCV 的 `FileStorage` 格式，这样 C++ 和 Python 都能直接读回：

```python
fs = cv2.FileStorage('result/calibration_result.yaml', cv2.FILE_STORAGE_WRITE)
fs.write('camera_matrix', K)
fs.release()
```

文件里至少包含以下字段，其中数值都是占位示例：

```yaml
image_width: 1280
image_height: 720
board_cols: 0                    # 你用的标定板规格，便于复现
board_rows: 0
square_size_mm: 0.

camera_matrix: !!opencv-matrix   # 3x3 内参矩阵
   rows: 3
   cols: 3
   dt: d
   data: [ 0., 0., 0., 0., 0., 0., 0., 0., 1. ]

distortion_coefficients: !!opencv-matrix   # [k1, k2, p1, p2, k3]
   rows: 1
   cols: 5
   dt: d
   data: [ 0., 0., 0., 0., 0. ]

rms_reprojection_error: 0.       # 总体 RMS，单位像素
mean_per_view_error: 0.          # 逐张误差的均值
max_per_view_error: 0.           # 最差的那张
worst_image: ""                  # 最差图片的文件名
per_view_errors: !!opencv-matrix # 逐张误差，长度 = 有效图片数
   rows: 0
   cols: 1
   dt: d
   data: [ ]

images_used: 0                   # 检测成功的图片数
images_total: 0
```

字段名可以调整，但要保证**能被程序读回**，并且**从文件本身就能看出标定质量和所用标定板的规格**。半年后别人拿到这个文件，应当不需要再问你任何问题。

另外，终端也要打印关键结果，不能只写文件。

### 任务 4：验证结果（10 分）

跑出一组数不等于标定正确。做两件事验证，并在报告里说明。

**看重投影误差。** 总体 RMS 落在什么区间？逐张误差里有没有个别图片明显偏大？如果有，原因是什么，你是怎么处理的？

**看去畸变效果。** 用 `cv2.undistort` 校正几张图片，与原图对比。图像边缘原本弯曲的直线有没有被拉直？

建议给脚本加一个开关，把角点检测图和去畸变前后的对比图输出到 `result/` 下便于查看。这部分不强制，但对你自己判断结果有帮助。

### 任务 5：位姿解算（PnP）（25 分）

标定求出的是内参和畸变，它们只描述相机本身。自瞄还需要知道**目标相对相机在什么位置、朝什么方向**，这由 PnP 求解。

> 原理见 [培训文档 12 的模块三](https://hello-world-vision.github.io/Vision_Website/induction-training/12-相机标定与位姿解算/)：PnP 的输入是 3D 模型点 + 2D 像素点 + 内参，输出是外参 `[R|t]`。

OpenCV 用 `cv2.solvePnP()` 求解，`flags` 可选 `SOLVEPNP_ITERATIVE`（默认）、`SOLVEPNP_IPPE`（平面目标专用）和 `SOLVEPNP_SQPNP`。**这三种在本题数据上的表现并不相同**，建议都跑一遍并写进报告。

`t` 的单位与 `objp` 一致：方格边长按 17mm 给出，`t` 的单位就是 mm。

#### 要做什么

在你标定好的内参基础上，对 `image/` 下每张成功检测到角点的图片解一次 PnP，输出：

1. 每张图的**平移向量** `tvec`，以及距离 `|tvec|`，单位 mm
2. 每张图的**重投影误差**：用 `cv2.projectPoints` 把 3D 角点按解出的 `[R|t]` 投影回图像，与实测角点比较。这个误差应当与标定阶段的逐张误差量级相当
3. 用解出的 `[R|t]` 反算棋盘格的物理尺寸，例如对角线长度。刚体变换不改变长度，所以它**必须等于由 9×6 格距 17mm 算出的 160.38mm**，这是一条严格的正确性校验

把结果写进 `result/pnp_result.yaml`，字段建议：

```yaml
board_true_diagonal_mm: 160.38       # 真值
pnp_repro_error_mean: 0.0            # PnP 重投影误差均值（px）
pnp_repro_error_max: 0.0
distance_range_mm: [0.0, 0.0]        # 距离范围
per_view:                             # 逐张
  - image: "0.jpg"
    distance_mm: 0.0
    repro_error: 0.0
    diagonal_mm: 0.0                 # 反算对角线，应≈160.38
```

#### 怎么判断结果是否正确

本题**不需要预先知道标准答案**，有三条互相独立的自检途径：

- **重投影误差**：应与标定阶段的逐张误差量级相当，约 0.2~0.4 px。明显偏大说明 `objp` 与角点顺序没有对齐，或者内参用错了
- **物理量级**：9×6 棋盘格、格距 17mm，板面尺寸为 136×85mm。手持标定板拍摄时距离在 15~30cm，解出的距离应当是几百毫米量级；反算的对角线应当等于 160.38mm
- **跨图一致性**：同一块板的物理尺寸不变，所以 41 张图反算的对角线应当高度一致，偏差小于 0.01mm。个别图片明显偏离，说明那几张的位姿解错了

用 `cv2.drawFrameAxes` 把 `rvec` 对应的坐标轴画到图上，可以直观看到板面朝向。这部分不强制。

#### 评分关注

- 能否说清 PnP 的输入（3D 模型点、2D 像素、内参）和输出（`[R|t]`）
- 能否用重投影误差和物理量级判断结果是否正确
- 能否解释 `t` 的单位由什么决定，以及为什么反算对角线可以作为严格校验
- 3D 与 2D 角点的顺序必须一一对应，错位会导致结果错误且不报错

> **关于角点顺序**：`findChessboardCorners` 返回的角点顺序是固定的，按行列扫描。PnP 用的 `objp` 要按同样顺序排列。如果标定阶段已经正确生成了 `objp`，这里直接复用。

### 额外探索部分

以下内容不计分，也不要求完成。如果你做完主任务还有时间，可以挑感兴趣的方向进一步尝试和学习：

- **算法对比**：把三种 `flags` 都跑一遍，统计各自的重投影误差均值和最大值。有没有哪一种在某几张图上明显偏大？原因是什么？提示：看看那几张图的板面倾角。
- **换算云台角度**：把 `tvec` 换算成目标的距离和偏转角，也就是自瞄要送给云台的 Yaw 与 Pitch。公式见培训文档模块三 3.4.2。
- **旋转表示互转**：用 `cv2.Rodrigues` 把 `rvec` 转成旋转矩阵，再转成欧拉角，观察板子翻转时哪个角度在变化。
- **模型选择**：对比固定 `k3=0`（`CALIB_FIX_K3`）和不固定的结果差异，判断这个镜头需不需要 `k3`。
- **图片数量的影响**：分别用 5 张、10 张、20 张和全部 41 张标定，观察内参如何收敛。多少张才够？

## 五、提交要求

### 1. 代码能跑

我们会在干净环境下执行：

```bash
cd Camera_Calibration
python3 calibrate.py --pnp
```

建议提交前自己从零走一遍，确认能跑通并在 `result/` 下生成结果文件。**没跑通也请提交**，在报告里说明你写到哪一步、卡在哪里、尝试过什么。不完整的代码配上清楚的说明，比留空提交得分更高。

依赖只允许 `opencv-python` 和 `numpy`。

### 2. 结果文件

- `result/calibration_result.yaml` — 内参、畸变、重投影误差
- `result/pnp_result.yaml` — 位姿解算结果（任务 5）
- 你认为有必要的验证图片，例如角点图、去畸变对比图、画了坐标轴的位姿图

**没做完也可以提交。** 把你做到哪一步、卡在哪里、尝试过什么写进一份简短报告（txt 或 md 均可）。不完整的代码配上清楚的说明，比留空提交得分更高。


## 六、我们看重什么

- **理解而非照搬。** 标定代码到处都能找到，我们会追问你为什么这么写：为什么方格边长不影响内参？为什么不能混用分辨率标定？亚像素精化为什么重要？
- **对结果的判断力。** 能跑出数字的人多，能判断这组数字是否可信的人少。重投影误差 0.3 与 3.0 分别意味着什么，你要能说清楚。PnP 部分我们没有给标准答案，需要你自己用重投影误差和物理量级来判断，实际工作中也是这样。
- **工程习惯。** 参数是否外置，异常情况是否处理，输出的文件半年后是否还能看懂。

## 七、参考资料

- **[入组培训 12-相机标定与位姿解算](https://hello-world-vision.github.io/Vision_Website/induction-training/12-相机标定与位姿解算/)** — 主要参考，本题知识点都在其中
- [OpenCV: calib3d 模块文档](https://docs.opencv.org/4.x/d9/d0c/group__calib3d.html) — 函数签名、`flags` 取值与畸变系数顺序的权威出处
- [OpenCV: Camera Calibration（Python 教程）](https://docs.opencv.org/4.x/dc/dbb/tutorial_py_calibration.html) — 含可运行示例

祝顺利。
