# 笔试题：神经网络车辆检测

> 建议用时 2~3 小时。允许查阅资料，允许参考开源实现，也鼓励用 AI 辅助理解代码和排查报错。用 AI 辅助的部分标注一下即可。

> **这题有多大**
>
> - **要你写的**：`src/detector.cpp` 里 4 个 TODO（第 80、99、135、166 行），约 65 行代码
> - **已给你的**：主流程、画框、图片遍历、结果统计，约 520 行，不用改
> - **那个 14MB 的模型**：已经训好了，你只管调用。不需要训练、不需要 GPU、不需要装 PyTorch
> - **第一步**：直接 `cmake -B build && cmake --build build -j4 && ./build/car_detector`，骨架现在就能跑
> - **卡住了**：每个 TODO 上面都有注释说明做什么，把注释和报错一起提供给 AI 提问

## 一、背景

雷达站俯视整个赛场，找出敌方机器人的位置并标在小地图上发给队友。这条链路的第一步是**在图像里检测出车辆**。

检测普遍使用神经网络，但训练好的模型只完成了一半工作。部署时你还要自己处理三件事：

1. **图片如何送入模型**。模型只接受固定尺寸的方形输入，相机给出的是 1600×1069 的长方形。直接拉伸成正方形会让车辆变形，检测框随之偏斜。
2. **网络输出如何解释**。输出是一个张量 `[1, 25200, 10]`，含 25200 个候选框，每框 10 个数。哪几个是坐标？用中心宽高还是左上右下表示？是原图坐标还是网络输入坐标？
3. **重复的框如何去除**。同一辆车往往被检出多个重叠框，需要只保留最可信的那个。

这三件事就是本题的全部内容。**不需要训练模型，也不需要 GPU**，模型已经训练完成并导出为 ONNX 随题下发。

> **先看培训文档**：[20-关于神经网络](https://hello-world-vision.github.io/Vision_Website/induction-training/20-关于神经网络/) 讲了模型的使用与 ONNX 接入，[10-OpenCV 基础与自瞄用法](https://hello-world-vision.github.io/Vision_Website/induction-training/10-opencv/) 讲了图像、像素与坐标。
>
> 工程前置：[09-CMake 基础](https://hello-world-vision.github.io/Vision_Website/induction-training/09-cmake基础/)。

## 二、已提供的内容

```
Car_Detector_Test/
├── QUESTION.md              # 本文件
├── CMakeLists.txt           # [完整] 编译配置
├── config/
│   └── params.yaml          # [★ 调参] 阈值等参数，改完不用重新编译
├── weights/
│   └── car.onnx             # [完整] 训练好的 YOLO 车辆检测模型（14 MB）
├── image/                   # [完整] 20 张赛场图片，1600×1069
├── include/
│   ├── detector.hpp         # [完整] 接口已给好
│   └── visualize.hpp        # [完整] 画框、配置读取，不用管
├── src/
│   ├── main.cpp             # [完整] 主流程，不用改
│   └── detector.cpp         # [★ 你的工作] 四个 TODO
└── result/                  # [★ 你的产出] 每次运行自动生成
```

**你只需要改两个文件**：`src/detector.cpp`（填四个 TODO）和 `config/params.yaml`（调阈值）。

图片遍历、模型加载、可视化绘制、结果保存和统计输出都已写好。

## 三、环境与编译

只需要 OpenCV，**不需要 PyTorch、CUDA 或 onnxruntime**：

```bash
sudo apt install build-essential cmake libopencv-dev
```

OpenCV 自带的 `dnn` 模块可以加载 ONNX 并在 CPU 上推理。编译运行：

```bash
cd Car_Detector_Test
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j4
./build/car_detector
```

骨架**可以直接编译运行**。但四个 TODO 还是空的，所有图片都会检出 0 个目标，填完才会出现检测框。

### 每次运行的产物

程序每跑一次，都会在 `result/` 下**新建一个带时间戳的目录，不会覆盖上一次的结果**：

```
result/20260921_143022_conf04/
├── 00.jpg ... 19.jpg    画好检测框的图（框 + 类别 + 置信度）
└── summary.txt          本次参数 + 每张图的检出数与推理耗时
```

常用命令行选项（会临时覆盖 `params.yaml`）：

```bash
./build/car_detector --tag baseline          # 给本次运行起名
./build/car_detector --conf 0.4              # 临时改置信度阈值
./build/car_detector --nms 0.3               # 临时改 NMS 阈值
./build/car_detector --image image/03.jpg    # 只跑单张，调试时用
./build/car_detector --help                  # 看全部选项
```

## 四、任务

### 任务 1：实现检测流程（60 分）

在 `src/detector.cpp` 里填四个 TODO。`detect()` 已经写好，它按顺序调用你的四个函数：

```
letterbox -> preprocess -> net.forward -> decode -> nms
```

| 函数 | 分值 | 要做的事 |
|------|-----:|---------|
| `letterbox()` | 12 | 等比缩放 + 灰边填充到方形，并记下 scale / pad |
| `preprocess()` | 8 | 转 blob：归一化、BGR→RGB、NCHW |
| `decode()` | 25 | 解析输出张量，取候选框，**坐标还原回原图** |
| `nms()` | 15 | 非极大值抑制，手写 IoU |

`decode()` 是本题最需要仔细处理的部分。动手前先把输出维度打印出来确认，不要靠猜：

```cpp
std::cout << output.size << std::endl;
```

本模型输出 `[1, 25200, 10]`。25200 是候选框数量；10 个通道中，前 4 个是框参数，第 5 个是该位置存在目标的置信度，后 5 个是类别分数。
各通道的具体含义、框参数用中心宽高还是角点表示，需要你自己确认：把某一行打印出来观察数值范围即可判断。

**评分关注**：维度理解是否正确，坐标还原有没有遗漏，零检测和框超出图像等边界情况是否处理，IoU 是否算对。

> 提示：框的位置整体偏移或缩放，通常是 letterbox 的坐标还原出了问题。这类错误在图上很容易看出来，跑一次即可确认。
>
> 另一个自检点：这批图 decode 后有几十到一百多个候选框，正常 NMS 之后每张只剩 3~7 个。
> 如果图上出现大量重叠的框，说明 NMS 没有生效。

### 任务 2：阈值调节（25 分）

`config/params.yaml` 里有两个阈值，改完不用重新编译。

做实验并在报告里回答：

1. `conf_threshold` 调大、调小分别会发生什么？各附一张图说明。
2. `nms_threshold` 调大、调小分别会发生什么？各附一张图说明。
3. 这批图里既有远处的小目标，也有彼此靠得很近的车。你最终选定的阈值是多少，为什么这组值适合这个场景？

> 参考：默认阈值下这 20 张图每张检出 3~7 辆车，合计约 100 个。
> 如果你的结果远多于或远少于这个量级，先检查实现，而不是先调阈值。

> 建议每次实验都用 `--tag` 起名，`result/` 下会积累一批带标记的结果，报告里可以直接引用。

**评分关注**：能否说清阈值背后的取舍，也就是漏检与误检、抑制不足与抑制过度之间的权衡，而不是只试出一组好看的数字。

### 任务 3：结果与报告（15 分）

提交：

- 调好阈值后的检测结果图，至少 10 张，能看出框贴合车辆
- 对应的 `summary.txt`
- 调参前后的对比图，如果有的话

### 额外探索部分

以下内容不计分，也不要求完成。如果你做完主任务还有时间，可以挑感兴趣的方向进一步尝试和学习：

- **手写 NMS 与 `cv::dnn::NMSBoxes` 对比**：两种都实现，比较结果和耗时是否一致。
- **耗时分析**：`summary.txt` 里记录了每张图的推理耗时，CPU 上 640 输入约 80ms。把 `input_size` 改成 1280，耗时和检出效果如何变化？实际部署该怎么选？
- **漏检分析**：挑一张有漏检的图，分析这辆车为什么没被检出：阈值、尺度还是遮挡？
- **处理视频**：把检测器接到一段视频上，自己找素材，输出带框的视频。
- **类别扩展**：如果模型要同时输出车辆颜色（红、蓝）和编号（1~5），`decode()` 需要怎么改？输出张量的形状会变成什么样？

## 五、提交要求

### 1. 代码能编译、能跑

我们会在干净环境下执行：

```bash
cd Car_Detector_Test
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j4
./build/car_detector
```

建议提交前自己从零走一遍，确认能编译。**没跑通也请提交**，在报告里说明你写到哪一步、卡在哪里、尝试过什么。不完整的代码配上清楚的说明，比留空提交得分更高。

记得保留你调好的 `config/params.yaml`，那是调参结果的凭据。

### 2. 分析报告（用你自己的话写）

一份文档，Markdown、PDF、txt 都行，不限格式不限长度。

**没做完也可以提交。** 把你做到哪一步、卡在哪里、尝试过什么写清楚即可。不完整的代码配上清楚的说明，比留空提交得分更高。

下面这些问题，挑你确实想清楚了的回答：

- 网络输出的维度是什么？你怎么确认的？每个通道代表什么？
- letterbox 的坐标还原你是怎么推导的？遗漏之后会怎样？最好附一张出错时的图。
- 两个阈值分别试了哪些值，图上出现了什么变化？
- 中间有没有卡住的地方？你是怎么定位的？

> **关于 AI**
>
> 允许并鼓励用 AI 帮你读代码、解释报错、查函数用法，用到的部分标注一下即可。但分析报告要写你自己的观察：
>
> 你打印出的维度是多少，框偏到了什么位置，你怎么发现的。
> 写得粗糙没关系，写得真实就好。没做出来的部分直接说明卡在哪里，这样写不扣分。

### 3. 结果图

`result/` 目录整个保留，或者把关键的几张单独放进报告，都可以。

## 六、我们看重什么

这道题的代码量不大（四个函数加起来约六十行），真正想看的是：

- **能否把模型跑起来**。这是部署的最小环节，比会训练更基础。
- **对张量维度和坐标系的敏感度**。这里最容易出错，也最能反映水平差异。
- **阈值背后的工程判断**。漏检和误检哪个代价更大，取决于下游怎么用这些结果。
- **诚实**。没做完的部分直接写明，做了一半并说清卡在哪里，比假装做完得分更高。

祝顺利。
